# OS
.DS_Store

# Personal / local
CLAUDE.local.md
.claude/settings.local.json

# Obsidian workspace state (personal UI state)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Raw source datasets (heavy binaries — kept on disk for agents, not versioned)
00_inbox/dataset_libia/

# Raw source dataset (terrorismo) — non versionato
00_inbox/dataset_terrorismo/
